{% extends 'base.html.twig' %}

{% block title %}
	New Product
{% endblock %}


{% block body %}
	{% if app.user %}
		<div class="container">
			<h1>Create new Product</h1>

			{{ include('product/_form.html.twig') }}

			<a class="btn btn-outline-warning" href="{{ path('app_product_index') }}">Back to list</a>
			<div/>
		{% endif %}
	</div>
{% endblock %}
